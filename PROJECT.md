# سیستم حضور و غیاب داروخانه — MVP

سند مرجع برای شروع توسعه. هدف: یک نسخهٔ ساده، کارآمد و قابل‌نصب روی Vercel که حضور/غیاب پرسنل را بر اساس لوکیشن ثبت کند.

---

## ۱. خلاصه محصول

وب‌اپلیکیشنی برای ثبت ورود و خروج پرسنل داروخانه.

- کاربر با یوزرنیم و رمز وارد می‌شود.
- دکمهٔ **ثبت ورود** / **ثبت خروج** را می‌زند.
- مرورگر موقعیت فعلی را می‌گیرد.
- سرور چک می‌کند کاربر داخل شعاع مجاز لوکیشن اختصاص‌داده‌شده هست یا نه.
- اگر بود → زمان ثبت می‌شود.
- اگر نبود → پیام: «باید حتماً در داروخانه حاضر باشید.»

ادمین در پنل جداگانه کاربران، لوکیشن‌ها و شیفت‌ها را مدیریت می‌کند و به هر کاربر لوکیشن و شیفت می‌دهد.

---

## ۲. محدوده MVP (چه چیزی هست / نیست)

### هست

- لاگین ساده (یوزرنیم + رمز)
- پنل ادمین: CRUD کاربر، لوکیشن، شیفت
- اختصاص لوکیشن و شیفت به کاربر
- ثبت ورود و خروج با اعتبارسنجی GPS
- لیست سادهٔ رکوردهای حضور (برای ادمین)
- دیپلوی روی Vercel
- رابط کاربری مدرن با تم دارک + گلس‌مورفیسم (جذاب و حرفه‌ای)

### نیست (فازهای بعدی)

- اپ موبایل Native
- تشخیص جعل GPS / anti-spoof پیشرفته
- گزارش‌های حقوق و اضافه‌کار پیچیده
- نوتیفیکیشن، مرخصی، درخواست اصلاح
- نقش‌های چندلایه (فقط `admin` و `employee`)
- چند شیفت هم‌زمان پیچیده برای یک نفر

---

## ۳. پیشنهاد استک (ساده و مناسب Vercel)

یک ریپو، یک اپ — کمترین پیچیدگی برای دیپلوی.

| لایه | انتخاب | دلیل |
|------|--------|------|
| فریم‌ورک | **Next.js (App Router) + React + TypeScript** | React + API در یک پروژه؛ دیپلوی مستقیم روی Vercel |
| UI | Tailwind CSS + کامپوننت‌های شیشه‌ای (Glass) | تم دارک مدرن و حرفه‌ای |
| موشن | CSS transition / Framer Motion سبک | حس مدرن بدون شلوغی |
| آیکون | Lucide React | ست آیکون تمیز |
| دیتابیس | **PostgreSQL** (Neon یا Vercel Postgres) | رایگان/ارزان و سازگار با Vercel |
| ORM | **Prisma** | مدل ساده و migration آسان |
| احراز هویت | Session با کوکی HTTP-only (مثلاً Auth.js / یا JWT ساده خودمان) | بدون سرویس سنگین |
| رمز عبور | bcrypt | استاندارد و کافی برای MVP |
| نقشه (ادمین) | Leaflet + OpenStreetMap یا فقط lat/lng دستی | بدون هزینه API اجباری |
| هاست | **Vercel** | مطابق نیاز پروژه |

> اگر بعداً بک‌اند جدا خواستید، می‌توان API را جدا کرد؛ برای MVP، Next.js کافی است.

---

## ۳.۵ سیستم طراحی — دارک + شیشه‌ای (الزامی)

UI باید **جذاب، مدرن و حرفه‌ای** باشد — نه قالب ادمین معمولی.

### جهت بصری

- **تم:** دارک به‌صورت پیش‌فرض
- **استایل:** Glassmorphism — پنل‌های مات روی پس‌زمینه اتمسفریک
- **حس:** آرام، حرفه‌ای (فضای داروخانه)، شبیه SaaS پریمیوم
- **پرهیز از:** داشبورد خاکستری تخت، بنفش/نئون کلیشه‌ای، glow زیاد

### توکن رنگ (پیشنهادی)

```css
:root {
  --bg-base: #070b14;
  --glass-bg: rgba(255, 255, 255, 0.06);
  --glass-border: rgba(255, 255, 255, 0.12);
  --glass-blur: 18px;
  --text-primary: #f4f7fb;
  --text-secondary: rgba(244, 247, 251, 0.68);
  --accent: #2dd4bf;
  --danger: #fb7185;
  --success: #34d399;
}
```

### قوانین UI

- لاگین: کارت شیشه‌ای وسط صفحه روی پس‌زمینه دارک اتمسفریک
- حضور: یک ترکیب متمرکز — هویت، وضعیت، یک دکمه بزرگ
- ادمین: سایدبار دارک + پنل‌های شیشه‌ای
- موبایل‌فرست برای اپ حضور؛ دسکتاپ‌فرست برای ادمین
- ۲–۳ موشن هدفمند (ورود صفحه، لودینگ دکمه، تغییر وضعیت)

جزئیات کامل توکن‌ها و کامپوننت‌ها: `PROJECT.en.md` بخش 4.

---

## ۴. نقش‌ها و صفحات

### ۴.۱ نقش‌ها

| نقش | دسترسی |
|-----|--------|
| `admin` | پنل ادمین کامل |
| `employee` | فقط اپ حضور/غیاب خودش |

### ۴.۲ اپ کاربر (مصرفی)

1. `/login` — ورود
2. `/` یا `/attendance` — صفحهٔ اصلی:
   - نمایش نام کاربر، لوکیشن، شیفت
   - وضعیت امروز: «وارد نشده» / «در محل» / «خارج شده»
   - دکمهٔ **ثبت ورود** یا **ثبت خروج**
   - پیام خطا در صورت خارج از محدوده

### ۴.۳ پنل ادمین

1. `/admin` — داشبورد خلاصه (اختیاری در MVP: فقط لینک‌ها)
2. `/admin/users` — لیست کاربران + ایجاد/ویرایش/حذف
3. `/admin/locations` — لیست لوکیشن‌ها + ایجاد/ویرایش/حذف
4. `/admin/shifts` — لیست شیفت‌ها + ایجاد/ویرایش/حذف
5. `/admin/attendance` — لیست رکوردهای ورود/خروج (فیلتر ساده: تاریخ، کاربر)

---

## ۵. مدل داده

### ۵.۱ User

| فیلد | نوع | توضیح |
|------|-----|--------|
| id | string/cuid | کلید اصلی |
| name | string | نام نمایشی |
| username | string | یکتا — برای لاگین |
| passwordHash | string | رمز هش‌شده |
| phone | string | شماره تلفن |
| role | enum | `admin` \| `employee` |
| locationId | string? | لوکیشن اختصاصی |
| shiftId | string? | شیفت اختصاصی |
| isActive | boolean | فعال/غیرفعال |
| createdAt | datetime | |
| updatedAt | datetime | |

### ۵.۲ Location

| فیلد | نوع | توضیح |
|------|-----|--------|
| id | string | |
| name | string | مثلاً «داروخانه مرکزی» |
| latitude | float | عرض جغرافیایی |
| longitude | float | طول جغرافیایی |
| radiusMeters | int | شعاع مجاز (پیشنهاد پیش‌فرض: ۱۵۰ متر) |
| address | string? | اختیاری |
| createdAt | datetime | |

### ۵.۳ Shift

| فیلد | نوع | توضیح |
|------|-----|--------|
| id | string | |
| name | string | مثلاً «صبح» / «عصر» |
| startTime | string | فرمت `HH:mm` مثلاً `08:00` |
| endTime | string | فرمت `HH:mm` مثلاً `16:00` |
| createdAt | datetime | |

> در MVP شیفت بیشتر جنبهٔ نمایشی/سازمانی دارد. اجبار سخت‌گیرانهٔ «فقط داخل ساعت شیفت اجازه ثبت» اختیاری است؛ پیشنهاد: در MVP فقط ذخیره و نمایش، بدون بلاک زمانی.

### ۵.۴ AttendanceRecord

| فیلد | نوع | توضیح |
|------|-----|--------|
| id | string | |
| userId | string | |
| locationId | string | لوکیشن در لحظهٔ ثبت |
| type | enum | `check_in` \| `check_out` |
| recordedAt | datetime | زمان سرور |
| clientLatitude | float | مختصات ارسال‌شده از کلاینت |
| clientLongitude | float | |
| distanceMeters | float | فاصله محاسبه‌شده تا مرکز لوکیشن |
| createdAt | datetime | |

---

## ۶. منطق کسب‌وکار

### ۶.۱ ثبت ورود (`check_in`)

1. کاربر لاگین باشد و `role = employee` (ادمین هم می‌تواند تست کند؛ اختیاری).
2. کاربر باید `locationId` داشته باشد؛ وگرنه خطا: «لوکیشن برای شما تعریف نشده.»
3. کلاینت GPS را می‌گیرد (`navigator.geolocation`).
4. اگر کاربر GPS را رد کند → خطا: «دسترسی به موقعیت مکانی لازم است.»
5. سرور فاصلهٔ Haversine بین مختصات کاربر و مرکز لوکیشن را حساب می‌کند.
6. اگر `distance > radiusMeters` → رد با پیام: **«باید حتماً در داروخانه حاضر باشید.»**
7. اگر داخل محدوده:
   - اگر آخرین رکورد باز همان روز `check_in` بدون `check_out` باشد → خطا: «قبلاً ورود ثبت شده.»
   - در غیر این صورت رکورد `check_in` ذخیره می‌شود.

### ۶.۲ ثبت خروج (`check_out`)

همان شروط لوکیشن + GPS.

شرط اضافه:

- باید یک `check_in` باز برای امروز (یا آخرین ورود بدون خروج) وجود داشته باشد؛ وگرنه: «ابتدا ورود را ثبت کنید.»

### ۶.۳ فرمول فاصله (Haversine)

```
R = 6371000  // شعاع زمین به متر
Δφ = (lat2 - lat1) بر حسب رادیان
Δλ = (lng2 - lng1) بر حسب رادیان
a = sin²(Δφ/2) + cos(φ1)·cos(φ2)·sin²(Δλ/2)
c = 2·atan2(√a, √(1−a))
distance = R·c
```

اگر `distance <= location.radiusMeters` → مجاز.

### ۶.۴ نکته دقت GPS

- در فضای بسته دقت GPS ضعیف است → `radiusMeters` را واقع‌بینانه بگذارید (۱۰۰–۲۰۰ متر).
- در UI به کاربر بگویید چند ثانیه صبر کند تا موقعیت دقیق‌تر شود.
- برای MVP همین کافی است؛ ضدجعل پیشرفته لازم نیست.

---

## ۷. API پیشنهادی

همه زیر `/api/...` در Next.js Route Handlers.

### Auth

| متد | مسیر | توضیح |
|-----|------|--------|
| POST | `/api/auth/login` | `{ username, password }` → session/cookie |
| POST | `/api/auth/logout` | خروج |
| GET | `/api/auth/me` | کاربر فعلی + لوکیشن + شیفت |

### Admin — Users

| متد | مسیر |
|-----|------|
| GET | `/api/admin/users` |
| POST | `/api/admin/users` |
| GET | `/api/admin/users/:id` |
| PUT | `/api/admin/users/:id` |
| DELETE | `/api/admin/users/:id` |

بدنهٔ ایجاد/ویرایش نمونه:

```json
{
  "name": "علی رضایی",
  "username": "ali",
  "password": "secret123",
  "phone": "09121234567",
  "role": "employee",
  "locationId": "...",
  "shiftId": "...",
  "isActive": true
}
```

### Admin — Locations

| متد | مسیر |
|-----|------|
| GET/POST | `/api/admin/locations` |
| PUT/DELETE | `/api/admin/locations/:id` |

```json
{
  "name": "داروخانه مرکزی",
  "latitude": 35.6892,
  "longitude": 51.3890,
  "radiusMeters": 150,
  "address": "تهران..."
}
```

### Admin — Shifts

| متد | مسیر |
|-----|------|
| GET/POST | `/api/admin/shifts` |
| PUT/DELETE | `/api/admin/shifts/:id` |

```json
{
  "name": "شیفت صبح",
  "startTime": "08:00",
  "endTime": "16:00"
}
```

### Attendance

| متد | مسیر | توضیح |
|-----|------|--------|
| POST | `/api/attendance/check-in` | `{ latitude, longitude }` |
| POST | `/api/attendance/check-out` | `{ latitude, longitude }` |
| GET | `/api/attendance/today` | وضعیت امروز کاربر جاری |
| GET | `/api/admin/attendance` | لیست برای ادمین (`?from=&to=&userId=`) |

پاسخ خطای خارج از محدوده (نمونه):

```json
{
  "ok": false,
  "code": "OUT_OF_RANGE",
  "message": "باید حتماً در داروخانه حاضر باشید.",
  "distanceMeters": 420,
  "allowedRadiusMeters": 150
}
```

---

## ۸. جریان UI کاربر

```
[لاگین]
   ↓
[صفحه حضور]
   ├── نمایش: نام، لوکیشن، شیفت، وضعیت امروز
   ├── اگر خارج از محدوده بعد از تلاش ثبت → پیام قرمز ثابت
   ├── دکمه ثبت ورود (اگر هنوز وارد نشده)
   └── دکمه ثبت خروج (اگر وارد شده و خارج نشده)
```

الزام‌های UX ساده:

- قبل از ثبت، درخواست دسترسی Location
- لودینگ روی دکمه تا پاسخ سرور
- موفقیت: پیام کوتاه + به‌روزرسانی وضعیت
- شکست خارج از محدوده: همان متن فارسی مشخص‌شده

---

## ۹. جریان UI ادمین

### کاربران

- فرم: نام، یوزرنیم، رمز، تلفن، نقش، لوکیشن، شیفت، فعال/غیرفعال
- در ویرایش: انتخاب لوکیشن و شیفت از dropdown

### لوکیشن‌ها

- نام + lat + lng + شعاع
- پیشنهاد MVP: فیلدهای عددی lat/lng + دکمه «استفاده از موقعیت فعلی من» برای ادمین
- اختیاری: نقشه Leaflet برای کلیک و انتخاب نقطه

### شیفت‌ها

- نام + ساعت شروع/پایان

### حضور و غیاب

- جدول: کاربر، نوع، زمان، لوکیشن، فاصله
- فیلتر تاریخ و کاربر

---

## ۱۰. Seed اولیه

بعد از اولین migrate، یک ادمین پیش‌فرض بسازید:

```
username: admin
password: (از env مثلاً ADMIN_BOOTSTRAP_PASSWORD)
role: admin
```

در README واقعی پروژه حتماً یادآوری شود که بعد از اولین ورود رمز عوض شود.

---

## ۱۱. متغیرهای محیطی

```env
DATABASE_URL=
AUTH_SECRET=
ADMIN_BOOTSTRAP_PASSWORD=
NEXT_PUBLIC_APP_URL=
```

---

## ۱۲. ساختار پوشه‌ها (پیشنهادی)

```
timesheet/
├── PROJECT.md                 ← سند فارسی
├── PROJECT.en.md              ← سند انگلیسی + Design System
├── prisma/
│   ├── schema.prisma
│   └── seed.ts
├── src/
│   ├── app/
│   │   ├── (auth)/login/page.tsx
│   │   ├── (app)/attendance/page.tsx
│   │   ├── admin/
│   │   │   ├── users/
│   │   │   ├── locations/
│   │   │   ├── shifts/
│   │   │   └── attendance/
│   │   └── api/
│   │       ├── auth/
│   │       ├── admin/
│   │       └── attendance/
│   ├── components/
│   ├── lib/
│   │   ├── auth.ts
│   │   ├── db.ts
│   │   ├── geo.ts          ← Haversine
│   │   └── validations.ts
│   └── types/
├── package.json
└── README.md
```

---

## ۱۳. فازهای توسعه (ترتیب پیشنهادی)

### فاز ۰ — اسکلت پروژه
- ساخت Next.js + Tailwind + Prisma
- اتصال دیتابیس
- مدل‌ها و migrate
- seed ادمین

### فاز ۱ — Auth
- لاگین / لاگ‌اوت / `me`
- محافظت مسیرهای `/admin/*` و `/attendance`

### فاز ۲ — پنل ادمین CRUD
- لوکیشن‌ها
- شیفت‌ها
- کاربران (+ اتصال لوکیشن و شیفت)

### فاز ۳ — حضور و غیاب
- گرفتن GPS در کلاینت
- API check-in / check-out
- منطق فاصله و پیام خارج از محدوده
- وضعیت امروز

### فاز ۴ — لیست رکوردها برای ادمین
- جدول + فیلتر ساده

### فاز ۵ — دیپلوی Vercel
- اتصال Neon/Postgres
- تنظیم env
- تست واقعی روی موبایل در محل داروخانه

---

## ۱۴. معیارهای پذیرش MVP

- [ ] ادمین می‌تواند لوکیشن با مختصات و شعاع بسازد
- [ ] ادمین می‌تواند شیفت بسازد
- [ ] ادمین می‌تواند کاربر بسازد و لوکیشن/شیفت بدهد
- [ ] کاربر لاگین می‌کند و فقط صفحه حضور را می‌بیند
- [ ] ثبت ورود داخل شعاع موفق است و زمان ذخیره می‌شود
- [ ] ثبت ورود خارج از شعاع با پیام فارسی مشخص رد می‌شود
- [ ] ثبت خروج با همان قواعد لوکیشن کار می‌کند
- [ ] ادمین لیست رکوردها را می‌بیند
- [ ] اپ روی Vercel بالا می‌آید و روی موبایل قابل استفاده است
- [ ] UI مطابق سیستم طراحی دارک + گلس (لاگین، حضور، ادمین) است

---

## ۱۵. تصمیم‌های معماری (خلاصه)

1. **یک اپ Next.js** به‌جای React جدا + بک‌اند جدا → ساده‌تر برای Vercel.
2. **اعتبارسنجی لوکیشن فقط سمت سرور** — کلاینت مختصات می‌فرستد؛ تصمیم نهایی با سرور است.
3. **شعاع ثابت روی هر لوکیشن** — قابل تنظیم توسط ادمین.
4. **زمان ثبت = زمان سرور** — کلاینت فقط مختصات می‌فرستد، نه ساعت دلخواه.
5. **MVP بدون اجبار ساعت شیفت** — شیفت برای نمایش و گزارش بعدی نگه داشته می‌شود.
6. **UI دارک + گلس بخشی از MVP است** — نه فاز پولیش بعدی.

---

## ۱۶. ریسک‌ها و راه‌حل MVP

| ریسک | راه‌حل ساده |
|------|-------------|
| GPS داخل ساختمان ضعیف است | شعاع ۱۵۰–۲۰۰ متر |
| کاربر Location را قطع می‌کند | بلاک ثبت + پیام واضح |
| جعل موقعیت (Fake GPS) | در MVP نادیده؛ بعداً می‌توان دقت/سرعت/IP را لاگ کرد |
| تفاوت ساعت دستگاه کاربر | همیشه از `recordedAt` سرور استفاده شود |
| HTTPS برای Geolocation | روی Vercel پیش‌فرض HTTPS است |

---

## ۱۷. شروع توسعه

از روی این سند:

1. پروژه Next.js را بسازید.
2. Prisma schema را مطابق بخش ۵ پیاده کنید.
3. فازها را به ترتیب ۱۳ پیش ببرید.
4. بعد از فاز ۳ روی گوشی واقعی در شعاع داروخانه تست کنید.

این فایل قرارداد محصول و معماری MVP است؛ در صورت تغییر scope، همین سند را اول به‌روز کنید.
